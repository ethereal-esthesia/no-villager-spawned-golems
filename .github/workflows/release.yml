name: Release

on:
  workflow_dispatch:
    inputs:
      confirm_release:
        description: Type "release" to publish the current Gradle pluginVersion.
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && inputs.confirm_release == 'release'

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Read release version
        id: version
        run: |
          version="$(./scripts/project-version.sh)"
          tag="v${version}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Refuse duplicate tag
        run: |
          if git rev-parse "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.version.outputs.tag }} already exists." >&2
            exit 1
          fi

      - name: Run Paper integration tests
        run: ./scripts/paper-integration-test.sh

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "$TAG" \
            build/libs/NoVillagerSpawnedGolems-"$VERSION".jar \
            --target "$GITHUB_SHA" \
            --title "NoVillagerSpawnedGolems $VERSION" \
            --notes "Release $VERSION. Paper integration tests passed before publishing."
